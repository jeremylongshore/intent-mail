{
  "mcpServers": {
    "intentmail": {
      "name": "intentmail",
      "description": "Self-hosted IntentMail MCP server for Gmail and Outlook OAuth, delta sync, AI-assisted review, local rules, and bounded provider actions. Runs locally over stdio; cloud AI features send selected message content to the configured provider.",
      "version": "0.5.1",
      "enabled": true,
      "transport": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/bin/intentmail.js", "serve"],
      "env": {}
    }
  }
}
